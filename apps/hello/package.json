{
  "name": "@basic-api/hello",
  "version": "1.0.0",
  "private": true,
  "main": "dist/handler.js",
  "scripts": {
    "clean": "rimraf dist",
    "build:js": "esbuild src/handler.ts --bundle --platform=node --target=node20 --format=cjs --outfile=dist/handler.js",
    "zip": "powershell -NoProfile -Command \"if (Test-Path dist/function.zip) { Remove-Item dist/function.zip }; Compress-Archive -Path dist/handler.js -DestinationPath dist/function.zip\"",
    "build": "npm run clean && npm run build:js && npm run zip"
  },
  "dependencies": {
    "@types/aws-lambda": "^8.10.162"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "rimraf": "^6.1.3",
    "typescript": "^6.0.3"
  }
}
